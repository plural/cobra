#tournament_anchor.col-12 data-tournament-id="#{@tournament.id}" data-user-id="#{current_user&.id}" data-user-name="#{current_user&.nrdb_username}"

= content_for :head do
  = vite_typescript_tag 'tournament', 'data-turbolinks-track': 'reload'
