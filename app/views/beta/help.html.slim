/ layout styles should be handled in svelte and not the ruby. fix later
#help_anchor.row style="margin-inline: 0;"

= content_for :head do
  = vite_typescript_tag 'help', 'data-turbolinks-track': 'reload'